<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.GomahRepoProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/lightBlack</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:statusBarColor">@color/lightBlack</item>
    </style>

    <style name="Theme.GomahRepoProject" parent="Base.Theme.GomahRepoProject" />
    
    <style name="Theme.GomahRepoProject.SplashScreen" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="postSplashScreenTheme">@style/Theme.GomahRepoProject</item>
    </style>

    <style name="CircularImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

</resources>